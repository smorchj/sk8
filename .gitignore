# Raw creategamecharacters.ai bundle (640MB, fetched with the owner's READ key
# — the key itself must NEVER appear in this repo). The game ships the
# optimized copy in assets/creator-min instead (characters carry the
# creategamecharacters licensing — free-game use permitted; see LICENSE).
assets/creator/

node_modules/
*.log

_scratch/
.playwright-mcp/

# the cut SFX one-shots: source takes are YouTube rips with no redistribution
# grant, and the game synthesises everything (js/sfx.js), so they never ship.
# tools/sfx-cut.mjs rebuilds them locally from the owner's folder.
assets/sfx/
